---
import { Separator } from "../ui/separator";

const { job } = Astro.props

interface Props {
  job: {
    title: string
    company: string
    period: string
    desc: string[]
  }
}
---

<div class="space-y-1">
  <h4 class="text-sm md:text-lg font-medium leading-none">
    {job.title}
  </h4>
  <p class="text-sm md:text-lg text-muted-foreground">{job.company}</p>
</div>
<Separator className="my-4" />
<div class="flex pt-2 h-24 items-center space-x-4 text-sm">
  <div>{job.period}</div>
  <Separator orientation="vertical" />
  <div>
    {job.desc.map(d => <p>{d}</p>)}
  </div>
</div>